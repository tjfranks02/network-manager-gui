export enum ElementStates {
  IDLE = 1,
  ACTIVE,
  FOLLOW_MOUSE,
  CLICKED,
  INCOMPLETE
};

export enum ConnectionStates {
};

export enum ElementTypes {
  NODE = "node",
  Connection = "connection",
};